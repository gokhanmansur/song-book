apply plugin: "war"

bootWar {
    mainClassName = "com.gm.songbook.SongBookApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
